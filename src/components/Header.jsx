import React from 'react';

const Header = () => {
    return (
        <div className="header">
            <a href="/" className="logo">Flight Search App</a>
        </div>
    );
}

export default Header;